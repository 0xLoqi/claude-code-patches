# Patch NN: [Name]

> Template for a new entry. Copy this folder to `NN-slug/`, fill it in, add the
> row to the top-level README table, then it ships with its LinkedIn post.
> A "patch" here is any reusable move: a behavior rule, a one-shot prompt, a
> multi-agent workflow, an MCP/hook setup, or a skill.

**What's broken (or slow) out of the box:** [The failure mode or the manual,
time-consuming way people do this today. Be concrete. If it's a speed play, name
the before: "this normally takes X."]

**The fix:** [One sentence on what this artifact does and the after: "now it takes
Y."]

## Install / Use

Paste this into Claude Code. [If it installs something, say so; if it's a prompt
you run as-is, say that.]

````
[THE ARTIFACT — the exact prompt / patch / workflow, copy-paste ready. One block.
If it appends a rule to CLAUDE.md, include the exact rule text and tell Claude to
read back which file it modified and where, then self-announce on first fire.]
````

## What changes

**Before:** [concrete example of the old behavior / the manual flow + how long it took]

**After:** [concrete example of the new behavior / the artifact running + how long it took]

## The trade-off

[What you give up. Every real technique costs something — more autonomy, a setup
step, a sharper exception list needed on prod. Name it honestly.]

---

*Drops with a LinkedIn post: [Elijah Wilbanks](https://www.linkedin.com/in/elijah-wilbanks/)*
